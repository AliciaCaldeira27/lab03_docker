{
  "name": "webacademy",
  "version": "1.0.0",
  "type": "module",   // Certifique-se de que isso esteja aqui
  "description": "Continuous Deployment with Github Actions, DockerHub",
  "scripts": {
    "start": "node app.js",
    "test": "jest --coverage"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^29.0.0",  // Use a versão mais recente
    "ts-jest": "^29.0.0", // Atualize para a versão mais recente
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
